
# Secrets: real env files are never committed. The value-less template
# backend/.env.example IS tracked -- it documents the variable names only.
.env

# Build
/backend.spec
!/backend/backend.spec
dist/
build/
!scripts/build/

# PyInstaller output
frontend/backend/
backend/dist/
backend/build/

# SQLite runtime files
backend/data/
*.db
*.db-shm
*.db-wal

# Electron build output
frontend/out/
frontend/.webpack/

# Node
node_modules/

# macOS
.DS_Store

# App data / model downloads (never commit)
backend/models/

.DS_Store
Thumbs.db

# SQLite runtime files
*.db-shm
*.db-wal
*.db-journal

# Temp files
*.tmp

*.vscode

# Documentation build artifacts
site/
reports/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
venv/
env/
ENV/
.venv/

# Temporary experimental directories (keep but ignore from version control)
# Used for testing build variants and experimental features
tmp_win_cu12/
tmp_winStartupErrorHandle/

# Runtime log files, rotated copies included
*.log
*.log.[0-9]*

# Bundled backend output (produced by PyInstaller build, not source)
frontend/backend/

# Internal dev session logs
docs/session-*.md

# Playwright CLI artefacts (local E2E exploration only)
.playwright-cli/
.playwright/
erudi-landing-loaded.yaml

# Local-only security report — details the app's attack surface; never commit it
# to the public repo (findings are tracked in issue #89).
security_best_practices_report.md