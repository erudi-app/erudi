# Development Dependencies
# Tools for development, testing, and debugging

# Testing
pytest==9.1.1
# pytest-asyncio 0.24 caps pytest<9, so it moves with the pytest major.
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-timeout==2.4.0  # a hung test must fail, not eat the whole job (#335)
httpx==0.28.1  # For FastAPI testing

# Code Quality
# ruff is both the linter and the formatter. Keep this pin in step with the
# `rev` in .pre-commit-config.yaml so the hooks and CI run the same version.
ruff==0.8.4

# Development Tools
ipython==8.31.0
ipdb==0.13.13

# Type stubs
types-python-dateutil==2.9.0.20241206

# Docs
mkdocs==1.6.1
zensical==0.0.59  # keep in sync with docs/requirements.txt
mkdocstrings[python]==1.0.6
pyyaml>=6.0

# Builds
cmake>=4.1.2